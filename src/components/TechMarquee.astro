---
const techs = ["Angular", "Spring Boot", "Java", "Astro", "SQL Server", "Tailwind CSS", "TypeScript", "REST APIs"];
---

<div class="relative flex overflow-x-hidden border-y border-white/5 bg-white/[0.01] py-10">
  <div class="animate-marquee whitespace-nowrap flex items-center">
    {techs.concat(techs).map((tech) => (
      <span class="mx-12 text-4xl md:text-6xl font-black uppercase tracking-tighter text-white/10 hover:text-nja-green transition-colors cursor-default">
        {tech}
      </span>
    ))}
  </div>

  <div class="absolute top-0 animate-marquee2 whitespace-nowrap flex items-center">
    {techs.concat(techs).map((tech) => (
      <span class="mx-12 text-4xl md:text-6xl font-black uppercase tracking-tighter text-white/10 hover:text-nja-green transition-colors cursor-default">
        {tech}
      </span>
    ))}
  </div>
</div>

<style>
  .animate-marquee { animation: marquee 30s linear infinite; }
  .animate-marquee2 { animation: marquee2 30s linear infinite; }
  @keyframes marquee { 0% { transform: translateX(0%); } 100% { transform: translateX(-100%); } }
  @keyframes marquee2 { 0% { transform: translateX(100%); } 100% { transform: translateX(0%); } }
</style>